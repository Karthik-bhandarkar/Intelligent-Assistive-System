# 🛠️ Setup Instructions

For quick setup, you can copy all these commands and paste them into your **Command Prompt (cmd)** one by one.

### 📋 All-in-One Commands (Quick Copy)
```cmd
cd "d:\onedrive\Desktop\image captioning and sign board detection"

python -m venv venv

.\venv\Scripts\activate

pip install -r requirements.txt

streamlit run streamlit_app.py
```

---

### 📝 Detailed Steps

1. **Navigate to Folder**
   ```cmd
   cd "d:\onedrive\Desktop\image captioning and sign board detection"
   ```

2. **Create Database (Virtual Environment)**
   ```cmd
   python -m venv venv
   ```

3. **Activate Environment**
   ```cmd
   .\venv\Scripts\activate
   ```

4. **Install Libraries**
   ```cmd
   pip install -r requirements.txt
   ```

5. **Run App**
   ```cmd
   streamlit run streamlit_app.py
   ```

---
### 🚀 Or just use the `run_project.bat` file!
